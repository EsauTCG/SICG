<style>
    :root {
        --brand-main: #7a0b0b;
        --brand-hover: #5c0808;
        --kpi-bg-page: #fdf5f4;
        --kpi-card-bg: #ffffff;
        --kpi-panel-bg: #fcf5f4;
        --kpi-panel-soft: #f8f9fa;
        --kpi-border: #ebdcdb;
        --kpi-border-strong: #e5c8c6;
        --kpi-text: #333333;
        --kpi-text-strong: #111827;
        --kpi-text-muted: #777777;
        --kpi-input-bg: #ffffff;
        --kpi-input-text: #212529;
        --kpi-input-placeholder: #9ca3af;
        --kpi-shadow: 0 4px 6px rgba(0,0,0,0.04);
        --kpi-shadow-strong: 0 12px 28px rgba(0,0,0,0.12);
        --kpi-active-bg: #fff5f5;
        --kpi-active-border: #ffcccc;
    }

    body[data-theme="dark"] {
        --brand-main: #b91c1c;
        --brand-hover: #dc2626;
        --kpi-bg-page: #0f172a;
        --kpi-card-bg: #111827;
        --kpi-panel-bg: #1f2937;
        --kpi-panel-soft: #172033;
        --kpi-border: rgba(255,255,255,0.12);
        --kpi-border-strong: rgba(248,113,113,0.28);
        --kpi-text: #e5e7eb;
        --kpi-text-strong: #f9fafb;
        --kpi-text-muted: #cbd5e1;
        --kpi-input-bg: #0b1220;
        --kpi-input-text: #f9fafb;
        --kpi-input-placeholder: #94a3b8;
        --kpi-shadow: 0 10px 26px rgba(0,0,0,0.32);
        --kpi-shadow-strong: 0 18px 38px rgba(0,0,0,0.48);
        --kpi-active-bg: rgba(185,28,28,0.16);
        --kpi-active-border: rgba(248,113,113,0.45);
    }

    body {
        background-color: var(--kpi-bg-page);
        color: var(--kpi-text);
        font-family: 'Segoe UI', Tahoma, sans-serif;
        font-size: 0.85rem;
    }

        body[data-theme="dark"] .layout-main-content {
            background: var(--kpi-bg-page) !important;
        }

            body[data-theme="dark"] .layout-main-content .layout-container-fluid {
                background: transparent !important;
                box-shadow: none !important;
            }

    .container-custom {
        max-width: 1200px;
        margin: 0 auto;
        color: var(--kpi-text);
    }

        .container-custom.form-width {
            max-width: 980px;
        }

    .top-banner {
        background: linear-gradient(135deg, #7a0b0b 0%, #5c0808 45%, #240404 100%);
        color: #ffffff;
        border-radius: 12px;
        padding: 1.5rem;
        margin-bottom: 1rem;
        box-shadow: var(--kpi-shadow);
        border: 1px solid rgba(255,255,255,0.10);
    }

    body[data-theme="dark"] .top-banner {
        background: linear-gradient(135deg, #7f1d1d 0%, #450a0a 45%, #111827 100%);
        border-color: rgba(248,113,113,0.20);
        box-shadow: var(--kpi-shadow-strong);
    }

    .top-banner h2,
    .top-banner p {
        color: #ffffff !important;
    }

    .top-banner .btn-outline-light {
        color: #ffffff !important;
        border-color: rgba(255,255,255,0.75) !important;
    }

        .top-banner .btn-outline-light:hover {
            background: rgba(255,255,255,0.16) !important;
            color: #ffffff !important;
        }

    .action-group {
        background-color: var(--kpi-panel-bg);
        border: 1px solid var(--kpi-border-strong);
        border-radius: 10px;
        padding: 0.75rem 1.2rem;
        display: flex;
        align-items: center;
        gap: 1rem;
        flex-grow: 1;
        box-shadow: var(--kpi-shadow);
    }

    body[data-theme="dark"] .action-group {
        background: linear-gradient(180deg, rgba(31,41,55,0.98), rgba(17,24,39,0.98));
    }

    .action-label {
        font-size: 0.75rem;
        font-weight: 800;
        color: var(--brand-main);
        text-transform: uppercase;
        border-right: 2px solid var(--kpi-border-strong);
        padding-right: 1rem;
        white-space: nowrap;
    }

    body[data-theme="dark"] .action-label {
        color: #fca5a5;
    }

    .btn-rojo {
        background-color: var(--kpi-card-bg);
        color: var(--brand-main);
        border: 1px solid var(--brand-main);
        padding: 6px 14px;
        border-radius: 6px;
        font-size: 0.8rem;
        font-weight: 700;
        text-decoration: none;
        transition: all 0.2s;
        display: inline-flex;
        align-items: center;
        gap: 5px;
    }

        .btn-rojo:hover {
            background-color: var(--brand-main);
            color: #ffffff;
            border-color: var(--brand-main);
            transform: translateY(-1px);
        }

    body[data-theme="dark"] .btn-rojo {
        background: rgba(15,23,42,0.92);
        color: #fca5a5;
        border-color: rgba(248,113,113,0.55);
    }

        body[data-theme="dark"] .btn-rojo:hover {
            background: rgba(185,28,28,0.92);
            color: #ffffff;
            border-color: rgba(248,113,113,0.90);
        }

    .btn-brand {
        background-color: var(--brand-main);
        color: #ffffff;
        border: none;
        padding: 6px 16px;
        border-radius: 20px;
        font-weight: 600;
        transition: 0.3s;
    }

        .btn-brand:hover {
            background-color: var(--brand-hover);
            color: #ffffff;
            transform: translateY(-1px);
        }

    .dashboard-panel {
        background-color: var(--kpi-card-bg);
        border-radius: 12px;
        border: 1px solid var(--kpi-border);
        margin-bottom: 1.5rem;
        box-shadow: var(--kpi-shadow);
        color: var(--kpi-text);
        overflow: hidden;
    }

    .dashboard-panel-head {
        background: var(--kpi-panel-bg);
        border-bottom: 1px solid var(--kpi-border);
        padding: 0.9rem 1rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.75rem;
        flex-wrap: wrap;
    }

    .dashboard-panel-title {
        font-weight: 800;
        color: var(--kpi-text-strong);
        margin: 0;
    }

    .dashboard-panel-subtitle {
        color: var(--kpi-text-muted);
        font-size: 0.78rem;
        margin: 0.15rem 0 0;
    }

    .filter-panel {
        background-color: var(--kpi-panel-soft);
        color: var(--kpi-text);
        border: 1px solid var(--kpi-border);
        border-radius: 10px;
        padding: 1rem;
    }

    .table-responsive {
        background: var(--kpi-card-bg);
        border-color: var(--kpi-border) !important;
    }

    .table-custom {
        color: var(--kpi-text);
        background: var(--kpi-card-bg);
    }

        .table-custom thead th {
            position: sticky;
            top: 0;
            z-index: 2;
            background-color: var(--kpi-panel-bg) !important;
            color: var(--kpi-text-muted);
            font-weight: 800;
            text-transform: uppercase;
            font-size: 0.75rem;
            border-bottom: 2px solid var(--kpi-border);
            padding: 12px;
        }

        .table-custom tbody td {
            background-color: var(--kpi-card-bg);
            color: var(--kpi-text);
            border-color: var(--kpi-border);
            vertical-align: middle;
        }

        .table-custom tbody tr:hover td {
            background-color: var(--kpi-active-bg);
        }

    body[data-theme="dark"] .table {
        --bs-table-bg: var(--kpi-card-bg);
        --bs-table-color: var(--kpi-text);
        --bs-table-border-color: var(--kpi-border);
        --bs-table-striped-bg: rgba(255,255,255,0.03);
        --bs-table-hover-bg: rgba(248,113,113,0.08);
    }

    .form-control,
    .form-select {
        background-color: var(--kpi-input-bg) !important;
        color: var(--kpi-input-text) !important;
        border: 1.5px solid var(--kpi-border) !important;
        border-radius: 6px;
        box-shadow: none !important;
    }

        .form-control:focus,
        .form-select:focus {
            outline: none;
            border-color: var(--brand-main) !important;
            box-shadow: 0 0 0 0.18rem rgba(185,28,28,0.18) !important;
        }

        .form-control::placeholder {
            color: var(--kpi-input-placeholder) !important;
        }

    body[data-theme="dark"] .form-select {
        color-scheme: dark;
    }

        body[data-theme="dark"] .form-select option {
            background: #111827;
            color: #f9fafb;
        }

    .container-custom .text-dark,
    .dashboard-panel .text-dark {
        color: var(--kpi-text-strong) !important;
    }

    .container-custom .text-muted,
    .dashboard-panel .text-muted {
        color: var(--kpi-text-muted) !important;
    }

    body[data-theme="dark"] .btn-outline-secondary,
    body[data-theme="dark"] .btn-outline-dark,
    body[data-theme="dark"] .btn-outline-primary,
    body[data-theme="dark"] .btn-outline-danger {
        color: #e5e7eb !important;
        border-color: rgba(203,213,225,0.45) !important;
        background: rgba(15,23,42,0.35) !important;
    }

        body[data-theme="dark"] .btn-outline-secondary:hover,
        body[data-theme="dark"] .btn-outline-dark:hover,
        body[data-theme="dark"] .btn-outline-primary:hover {
            color: #ffffff !important;
            background: rgba(185,28,28,0.65) !important;
            border-color: rgba(248,113,113,0.70) !important;
        }

        body[data-theme="dark"] .btn-outline-danger:hover {
            color: #ffffff !important;
            background: rgba(220,38,38,0.75) !important;
            border-color: rgba(248,113,113,0.90) !important;
        }

    .kpi-thumb {
        width: 76px;
        height: 44px;
        object-fit: cover;
        border-radius: 8px;
        border: 1px solid var(--kpi-border);
        box-shadow: var(--kpi-shadow);
        background: var(--kpi-panel-soft);
    }

    .kpi-preview {
        width: 220px;
        height: 130px;
        object-fit: cover;
        border-radius: 10px;
        border: 1px solid var(--kpi-border);
        box-shadow: var(--kpi-shadow);
        background: var(--kpi-panel-soft);
    }

    .status-pill {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        border-radius: 999px;
        padding: 0.3rem 0.65rem;
        font-size: 0.75rem;
        font-weight: 800;
        border: 1px solid transparent;
    }

        .status-pill.on {
            background: rgba(25,135,84,0.12);
            color: #198754;
            border-color: rgba(25,135,84,0.25);
        }

        .status-pill.off {
            background: rgba(220,53,69,0.12);
            color: #dc3545;
            border-color: rgba(220,53,69,0.25);
        }

    body[data-theme="dark"] .status-pill.on {
        background: rgba(34,197,94,0.14);
        color: #bbf7d0;
        border-color: rgba(34,197,94,0.35);
    }

    body[data-theme="dark"] .status-pill.off {
        background: rgba(248,113,113,0.14);
        color: #fecaca;
        border-color: rgba(248,113,113,0.35);
    }

    .stat-card {
        background: var(--kpi-card-bg);
        border: 1px solid var(--kpi-border-strong);
        border-radius: 10px;
        padding: 0.75rem 1rem;
        min-width: 150px;
        box-shadow: var(--kpi-shadow);
    }

        .stat-card .stat-label {
            color: var(--kpi-text-muted);
            font-size: 0.72rem;
            font-weight: 800;
            text-transform: uppercase;
        }

        .stat-card .stat-value {
            color: var(--kpi-text-strong);
            font-size: 1.25rem;
            font-weight: 900;
            line-height: 1;
            margin-top: 0.3rem;
        }

    .empty-state {
        text-align: center;
        color: var(--kpi-text-muted);
        padding: 3rem 1rem;
    }

        .empty-state i {
            font-size: 2rem;
            display: block;
            margin-bottom: 0.75rem;
            opacity: 0.6;
        }

    body[data-theme="dark"] code {
        color: #fca5a5;
    }

    @@media (max-width: 992px) {
        .action-group {
            flex-direction: column;
            align-items: stretch;
            text-align: center;
        }

        .action-label {
            border-right: none;
            border-bottom: 2px solid var(--kpi-border-strong);
            padding-right: 0;
            padding-bottom: 0.5rem;
            margin-bottom: 0.5rem;
        }

        .d-flex.gap-2 {
            flex-wrap: wrap;
            justify-content: center;
        }
    }

    @@media (max-width: 576px) {
        .container-custom {
            padding-left: 0.75rem !important;
            padding-right: 0.75rem !important;
        }

        .top-banner {
            padding: 1rem;
        }

            .top-banner h2 {
                font-size: 1.25rem;
            }

        .dashboard-panel-head {
            align-items: flex-start;
        }

        .kpi-preview {
            width: 100%;
            height: 180px;
        }

        .stat-card {
            width: 100%;
        }
    }
</style>